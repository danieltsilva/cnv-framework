# Caiu No Vestibular Project

Framework Spring-Boot based to create a REST API for educational questions ans exams.

To run the application with Docker:


### DB (with Docker)
```
$ docker run --name cnv-mysql -v cnv-test:/var/lib/mysql -p 3306:3306 -e MYSQL_ROOT_PASSWORD=p4ssw0rd -d mysql:5.7
```